import 'package:flutter/material.dart';

class Sirkeci extends StatelessWidget {
  const Sirkeci({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
