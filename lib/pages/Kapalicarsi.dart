import 'package:flutter/material.dart';

class Kapalicarsi extends StatelessWidget {
  const Kapalicarsi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
